---
title: "MySQL"
description: "This page explains how data types are mapped from a source MySQL database to target databases. Please visit the [MySQL Data Types Documentation](https://dev.mysql.com/doc/refman/8.4/en/data-types.html) to get a primer on the various MySQL datatypes. "
icon: "gear"
---

### Mapping

The following table explains how the MySQL datatypes are mapped to the target databases.

| MySQL                | PostgreSQL                 | Snowflake |
| :------------------- | :------------------------- | :-------- |
| `BIT`                | `BIT`                      | `BINARY`       |
| **Numbers**          |                            |           |
| `TINYINT`            | `SMALLINT`                 | `TINYINT`       |
| `SMALLINT`           | `SMALLINT`                 | `NUMBER`          |
| `MEDIUMINT`          | `INT`                      | `NUMBER`          |
| `INT`                | `INT`                      | `INT`         |
| `BIGINT`             | `BIGINT`                   | `BIGINT`          |
| `DECIMAL`            | `NUMERIC`                  | `NUMBER`          |
| `FLOAT`              | `FLOAT`                    | `FLOAT`          |
| `DOUBLE`             | `DOUBLE PRECISION`         | `FLOAT`          |
| **Numbers Unsigned** |                            |           |
| `TINYINT UNSIGNED`   | `SMALLINT`                 | `NUMBER`          |
| `SMALLINT UNSIGNED`  | `INT`                      | `NUMBER`          |
| `MEDIUMINT UNSIGNED` | `INT`                      | `NUMBER`          |
| `INT UNSIGNED`       | `BIGINT`                   | `NUMBER`          |
| `BIGINT UNSIGNED`    | `NUMERIC(21)`              | `NUMBER`          |
| `DECIMAL UNSIGNED`   | `NUMERIC(N+1)`             | `NUMBER`          |
| `FLOAT UNSIGNED`     | `DOUBLE PRECISION`         | `FLOAT`          |
| `DOUBLE UNSIGNED`    | `N/A`                      | `N/A`          |
| **Times**            |                            |           |
| `DATE`               | `DATE`                     | `DATE`          |
| `TIME`               | `TIME`                     | `TIME`          |
| `DATETIME`           | `TIMESTAMP`                | `TIMESTAMP_NTZ` |
| `TIMESTAMP`          | `TIMESTAMP WITH TIME ZONE` | `TIMESTAMP_LTZ` |
| `YEAR`               | `SMALLINT`                 | `INT`          |
| **Strings**          |                            |           |
| `CHAR`               | `CHAR`                     |           |
| `VARCHAR`            | `VARCHAR`                  |           |
| `TINYTEXT`           | `TEXT`                     |           |
| `MEDIUMTEXT`         | `TEXT`                     |           |
| `TEXT`               | `TEXT`                     |           |
| `LONGTEXT`           | `TEXT`                     |           |
| **Blobs**            |                            |           |
| `BINARY`             | `BYTEA`                    |           |
| `VARBINARY`          | `BYTEA`                    |           |
| `TINYBLOB`           | `BYTEA`                    |           |
| `MEDIUMBLOB`         | `BYTEA`                    |           |
| `BLOB`               | `BYTEA`                    |           |
| `JSON`               | `JSON`                     |           |
| **Sets**             |                            |           |
| `ENUM`               | `VARCHAR(64)`              |           |
| `SET`                | `VARCHAR(64)`              |           |